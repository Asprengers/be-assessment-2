<% include header.ejs %>
    <% include nav.ejs %>
        <title>
            <%= data.id %> - Book A Date</title>
        <h1>
            <%= data.Subject %>
        </h1>
        <div class="feed">

          <p>
              <%= data.Subject %>
          </p>
                    <p>
                        <%= data.message %>
                    </p>
                    <% if (user) { %>

                        <p class="buttons">
                          <button id=remover-message data-id="<%= data.id %>" class=buttonRemove>Remove</button>

                        </p>
                        <% } %>
        </div>
        <p> <a href="/chat"> Terug</a></p>

        <% include footer.ejs %>
