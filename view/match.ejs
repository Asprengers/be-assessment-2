<% include header.ejs %>
    <% include nav.ejs %>
        <title>
            <%= data.name %> - Book A Date</title>
        <h1>
            <%= data.name %>
        </h1>
        <% if (data.cover) { %>
            <img alt src="/upload/<%= data.cover %>">
            <% } %>
                <p>
                    <%= data.book %>
                </p>
                <% if (user) { %>

                    <p>
                        <button id=js-remove data-id="<%= data.id %>" class=buttonRemove>Remove</button>
                    </p>
                    <% } %>
                        <p> <a href="/afterhome"> Terug</a></p>

                        <% include footer.ejs %>
