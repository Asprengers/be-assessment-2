<% include header.ejs %>
    <% include nav.ejs %>
        <title>
            <%= data.name %> - Book A Date</title>
        <h1>
            <%= data.name %>
        </h1>
        <div class="feed1">
            <% if (data.cover) { %>
                <img alt src="/upload/<%= data.cover %>">
                <% } %>
                    <p>
                        <%= data.book %>
                    </p>
                    <% if (user) { %>

                        <p class="buttons">
                            <button id=js-remove data-id="<%= data.id %>" class=buttonRemove>Remove</button>
                            <a href="/chat" class="chatbutton">Chat</a>
                        </p>
                        <% } %>
        </div>
        <p> <a href="/"> Terug</a></p>

        <% include footer.ejs %>
